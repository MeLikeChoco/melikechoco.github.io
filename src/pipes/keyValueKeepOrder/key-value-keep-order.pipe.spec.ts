import { KeyValueKeepOrderPipe } from './key-value-keep-order.pipe';

describe('KeyValueKeepOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyValueKeepOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
