import { StylesheetDirective } from './stylesheet.directive';

describe('StylesheetDirective', () => {
  it('should create an instance', () => {
    const directive = new StylesheetDirective();
    expect(directive).toBeTruthy();
  });
});
